// Attendance marking controller stub

module.exports = {
  async markAttendance(req, res) {
    res.json({ message: 'markAttendance stub' });
  },

  async getAttendance(req, res) {
    res.json({ message: 'getAttendance stub' });
  }
};
