// Academic admin (HOD/TP) controller stub

module.exports = {
  async createTimetable(req, res) {
    res.json({ message: 'createTimetable stub' });
  },

  async assignFaculties(req, res) {
    res.json({ message: 'assignFaculties stub' });
  }
};
