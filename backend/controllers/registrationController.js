// Registration handling controller stub

module.exports = {
  async registerForEvent(req, res) {
    res.json({ message: 'registerForEvent stub' });
  },

  async cancelRegistration(req, res) {
    res.json({ message: 'cancelRegistration stub' });
  }
};
