// Department operations controller stub

module.exports = {
  async createDepartment(req, res) {
    res.json({ message: 'createDepartment stub' });
  },

  async listDepartments(req, res) {
    res.json({ message: 'listDepartments stub' });
  }
};
