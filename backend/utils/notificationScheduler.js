// Scheduled notification helper stub
// TODO: integrate node-cron or agenda

module.exports = {
  scheduleNotification(jobId, when, payload) {
    // Placeholder: schedule a job
    return { jobId, scheduled: true };
  }
};
