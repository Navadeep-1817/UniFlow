// Excel export stub
// TODO: use exceljs to produce spreadsheets

module.exports = {
  async exportToExcel(rows, columns) {
    // Placeholder: return Buffer or path
    return Buffer.from('XLSX');
  }
};
