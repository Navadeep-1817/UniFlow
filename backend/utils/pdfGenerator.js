// PDF generator stub
// TODO: use pdf-lib or puppeteer to render PDFs

module.exports = {
  async generatePdf(htmlOrData, options = {}) {
    // Placeholder: return Buffer or path
    return Buffer.from('PDF');
  }
};
